# Чеклист для деплоя на Vercel

Используйте этот чеклист перед деплоем проекта.

## ✅ Предварительная подготовка

### Локальная разработка
- [ ] Проект запускается локально (`npm run dev`)
- [ ] Сборка проходит успешно (`npm run build`)
- [ ] Нет ошибок TypeScript (`npm run lint`)
- [ ] Все зависимости установлены

### Supabase
- [ ] Создан проект в Supabase
- [ ] Выполнен SQL скрипт `supabase-schema.sql`
- [ ] Таблицы созданы и заполнены начальными данными
- [ ] Получены все ключи (URL, Anon Key, Service Role Key)

### Git репозиторий
- [ ] Код закоммичен в Git
- [ ] Репозиторий подключен к GitHub/GitLab/Bitbucket
- [ ] `.env.local` добавлен в `.gitignore` (не коммитится)

## ✅ Настройка Vercel

### Импорт проекта
- [ ] Проект импортирован в Vercel
- [ ] Выбрана правильная ветка (обычно `main` или `master`)
- [ ] Framework автоматически определён как Next.js

### Переменные окружения
- [ ] `NEXT_PUBLIC_SUPABASE_URL` добавлена
- [ ] `NEXT_PUBLIC_SUPABASE_ANON_KEY` добавлена
- [ ] `SUPABASE_SERVICE_ROLE_KEY` добавлена (без `NEXT_PUBLIC_`!)
- [ ] `TELEGRAM_BOT_TOKEN` добавлена (если используется)
- [ ] `TELEGRAM_CHAT_ID` добавлена (если используется)
- [ ] Все переменные установлены для Production, Preview и Development

### Build Settings
- [ ] Framework Preset: Next.js
- [ ] Build Command: `npm run build` (по умолчанию)
- [ ] Output Directory: `.next` (по умолчанию)
- [ ] Install Command: `npm install` (по умолчанию)

## ✅ Первый деплой

- [ ] Деплой запущен
- [ ] Сборка прошла успешно (проверить логи)
- [ ] Сайт доступен по URL Vercel
- [ ] Нет ошибок в консоли браузера

## ✅ Проверка функционала

### Главная страница
- [ ] Страница загружается
- [ ] Все секции отображаются
- [ ] Стили применяются корректно
- [ ] Анимации работают
- [ ] Изображения загружаются

### Админ-панель
- [ ] Доступна по `/admin`
- [ ] Вход работает (пароль по умолчанию: `admin123`)
- [ ] Портфолио: просмотр, создание, редактирование, удаление
- [ ] Настройки сайта: редактирование и сохранение
- [ ] Документы: редактирование и сохранение
- [ ] Блоки сайта: включение/выключение работает
- [ ] Смена пароля работает

### API эндпоинты
- [ ] `/api/admin/settings` - возвращает настройки
- [ ] `/api/admin/portfolio` - возвращает портфолио
- [ ] `/api/documents` - возвращает документы
- [ ] `/api/admin/auth` - аутентификация работает

### Форма обратной связи
- [ ] Форма отправляется (если настроен Telegram)
- [ ] Заявки приходят в Telegram (если настроено)

## ✅ Безопасность

- [ ] Пароль администратора изменён с дефолтного
- [ ] Используется сильный пароль
- [ ] `.env.local` не закоммичен в репозиторий
- [ ] Все секреты в переменных окружения Vercel

## ✅ Оптимизация (опционально)

- [ ] Настроен кастомный домен (если нужен)
- [ ] Настроена аналитика (если нужна)
- [ ] Проверена производительность (Lighthouse)

## ⚠️ Известные ограничения

- [ ] **Загрузка файлов**: `/api/upload` не работает на Vercel
  - Нужно перейти на Supabase Storage или другой сервис
  - Пока можно использовать только URL изображений

## 📝 После деплоя

- [ ] Сохранены все важные данные (URL, ключи)
- [ ] Документация обновлена
- [ ] Команда уведомлена о деплое

---

**Готово к деплою!** 🚀

Если все пункты отмечены, можно деплоить проект.

