# Деплой на Vercel

Пошаговая инструкция по деплою проекта на Vercel.

## 📋 Предварительные требования

1. Аккаунт на [Vercel](https://vercel.com)
2. Проект в [Supabase](https://supabase.com) (уже настроен)
3. Telegram бот (опционально, для отправки заявок)

## 🚀 Шаги деплоя

### 1. Подготовка репозитория

Убедитесь, что ваш код находится в Git репозитории (GitHub, GitLab или Bitbucket):

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin <ваш-репозиторий>
git push -u origin main
```

### 2. Импорт проекта в Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите **"Add New..."** → **"Project"**
3. Импортируйте ваш Git репозиторий
4. Vercel автоматически определит Next.js проект

### 3. Настройка переменных окружения

В настройках проекта Vercel перейдите в **Settings** → **Environment Variables** и добавьте:

#### Обязательные переменные (Supabase):

```
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here
```

#### Опциональные переменные (Telegram):

```
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
```

**Важно:**
- Установите переменные для всех окружений: **Production**, **Preview**, **Development**
- `SUPABASE_SERVICE_ROLE_KEY` должна быть **секретной** (не имеет префикса `NEXT_PUBLIC_`)

### 4. Настройка Build Settings

Vercel автоматически определит настройки для Next.js, но убедитесь что:

- **Framework Preset:** Next.js
- **Build Command:** `npm run build` (по умолчанию)
- **Output Directory:** `.next` (по умолчанию)
- **Install Command:** `npm install` (по умолчанию)

### 5. Деплой

1. Нажмите **"Deploy"**
2. Дождитесь завершения сборки
3. После успешного деплоя вы получите URL вида: `https://your-project.vercel.app`

### 6. Настройка домена (опционально)

1. В настройках проекта перейдите в **Settings** → **Domains**
2. Добавьте ваш домен
3. Следуйте инструкциям по настройке DNS

## 🔧 Проверка после деплоя

### 1. Проверка главной страницы

Откройте ваш сайт и убедитесь, что:
- ✅ Страница загружается
- ✅ Все компоненты отображаются корректно
- ✅ Стили применяются правильно

### 2. Проверка админ-панели

1. Перейдите на `/admin`
2. Войдите с паролем по умолчанию: `admin123`
3. Проверьте работу всех разделов:
   - Портфолио (создание, редактирование, удаление)
   - Настройки сайта
   - Документы
   - Блоки сайта
   - Смена пароля

### 3. Проверка API

Проверьте работу API эндпоинтов:
- `/api/admin/settings` - должен возвращать настройки
- `/api/admin/portfolio` - должен возвращать портфолио
- `/api/documents` - должен возвращать документы

### 4. Проверка отправки заявок (если настроен Telegram)

1. Заполните форму обратной связи
2. Проверьте, что заявка приходит в Telegram

## ⚠️ Важные замечания

### Загрузка файлов

⚠️ **Текущая реализация загрузки файлов не будет работать на Vercel!**

API `/api/upload` использует файловую систему, которая доступна только для чтения на Vercel. Для продакшена нужно:

1. **Использовать Supabase Storage** (рекомендуется)
2. Или другой сервис хранения файлов (AWS S3, Cloudinary и т.д.)

### Безопасность

- ✅ Обязательно смените пароль администратора после первого входа
- ✅ Используйте сильный пароль
- ✅ Не коммитьте `.env.local` в репозиторий
- ✅ Используйте переменные окружения Vercel для всех секретов

### Производительность

- Vercel автоматически оптимизирует Next.js приложения
- Изображения оптимизируются через Next.js Image
- API роуты работают как Serverless функции

## 🔄 Обновление проекта

После каждого push в основную ветку Vercel автоматически:
1. Запустит новую сборку
2. Развернёт изменения
3. Создаст preview URL для pull requests

## 📊 Мониторинг

В панели Vercel вы можете:
- Просматривать логи деплоев
- Мониторить производительность
- Настраивать аналитику
- Управлять переменными окружения

## 🐛 Устранение проблем

### Ошибка сборки

1. Проверьте логи в панели Vercel
2. Убедитесь, что все зависимости установлены
3. Проверьте, что TypeScript компилируется без ошибок

### Ошибка "Missing Supabase environment variables"

1. Проверьте, что все переменные окружения добавлены в Vercel
2. Убедитесь, что переменные установлены для нужного окружения
3. Перезапустите деплой после добавления переменных

### Ошибка при загрузке файлов

Это ожидаемо - файловая система на Vercel доступна только для чтения. Нужно перейти на Supabase Storage или другой сервис.

## 📚 Дополнительные ресурсы

- [Документация Vercel](https://vercel.com/docs)
- [Документация Next.js](https://nextjs.org/docs)
- [Документация Supabase](https://supabase.com/docs)

---

Успешного деплоя! 🚀

